Reserved for real pixel-art PNG sprite sheets. Sprites currently render
procedurally from `src/lib/sprites.ts` (colored pixel blocks) — drop art
assets here and swap the renderer over to `<Image>`/`drawImage` when ready.
